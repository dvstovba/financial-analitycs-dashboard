#Setup
- npm i
- npm i -g json-server

#Start
- npm run serve
###broadcast to WLAN
   in package.json -> scripts -> serve
   and in src/config.js -> URI
   change "localhost" to your local IP

- npm run serve
